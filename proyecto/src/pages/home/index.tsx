import { LayoutClient } from "@/Layout";
import { Main } from "@/root/components";
export default function Home() {
    return (
        <LayoutClient>
            <Main />
        </LayoutClient>
    );
}
